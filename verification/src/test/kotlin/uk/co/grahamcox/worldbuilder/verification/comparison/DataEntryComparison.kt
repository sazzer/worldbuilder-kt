package uk.co.grahamcox.worldbuilder.verification.comparison

import uk.co.grahamcox.worldbuilder.verification.DataTableEntry

/**
 * Mechanism by which we can compare a set of data entries to an object
 */
class DataEntryComparison {
    /**
     * Compare the provided object to what we expect to see if they match
     * @param expected The expected set of values
     * @param actual The actual object to compare against
     */
    fun compare(expected: Collection<DataTableEntry>, actual: Any) {

    }
}