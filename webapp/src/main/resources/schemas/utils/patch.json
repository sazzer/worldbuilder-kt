{
  "$schema": "http://jsonschema.org/draft-04/schema#",

  "title": "JSON Patch Schema",
  "type": "array",
  "items": {
    "title": "patch_entry",
    "description": "A single patch action to perform",
    "type": "object",
    "properties": {
      "op": {
        "enum": [
          "test",
          "remove",
          "add",
          "replace",
          "move",
          "copy"
        ]
      },
      "path": {
        "description": "The path to the field in the JSON Object",
        "type": "string"
      },
      "value": {
        "description": "The value to use for the field."
      },
      "from": {
        "description": "The path to the field to move or copy from",
        "type": "string"
      }
    },
    "required": [
      "op",
      "path"
    ],
    "additionalProperties": false
  }
}